language: node_js
node_js:
  - "10"
  - "9"
  - "8"

install:
  - npm install

before_script:
  - npm install -g jsinspect
  - npm install -g bundlesize

script:
  - npm run build
  - npm run lint
  - jsinspect ./src
  - bundlesize

branches:
  only:
  - master
